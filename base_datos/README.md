# Base de datos

Este proyecto usa las migraciones de Django como fuente principal de estructura de base de datos.

La base local `db.sqlite3` no se sube al repositorio. En PythonAnywhere se ejecutaran `migrate` y `bootstrap_users` para preparar la instalacion.
